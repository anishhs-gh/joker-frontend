# Development server port
PORT=3001

# API Configuration
# The base URL for the mock API backend (without trailing slash)
REACT_APP_API_URL=http://localhost:3000

# WebSocket URL for real-time logs (without trailing slash)
REACT_APP_WS_URL=ws://localhost:3000
